allow:
    - write=app.log

scripts:
    start:
        desc: launch dev server
        cmd: deno task start
    # test: deno test
    lint: deno lint
    fmt: deno fmt
    pre-commit:
        cmd:
            - vr lint
            - vr fmt
        gitHook: pre-commit
